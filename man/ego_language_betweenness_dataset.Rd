% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ego_language_betweenness_dataset.R
\name{ego_language_betweenness_dataset}
\alias{ego_language_betweenness_dataset}
\title{Compute ego language betweenness for all egos in a dataset}
\usage{
ego_language_betweenness_dataset(
  ego_df,
  alter_df,
  edge_df,
  ego_id_col = "networkCanvasEgoUUID",
  alter_id_col = "networkCanvasUUID",
  source_col = "networkCanvasSourceUUID",
  target_col = "networkCanvasTargetUUID",
  ego_name = "ego",
  community_attr = "languageUsedCategory",
  unknown_community = "Unknown",
  participant_col = "participant"
)
}
\arguments{
\item{ego_df}{A data frame containing ego-level data.}

\item{alter_df}{A data frame containing alter-level data.}

\item{edge_df}{A data frame containing edge-level data.}

\item{ego_id_col}{Column name in \code{ego_df} containing ego IDs.}

\item{alter_id_col}{Column name in \code{alter_df} containing alter IDs.}

\item{source_col}{Column name in \code{edge_df} containing source alter IDs.}

\item{target_col}{Column name in \code{edge_df} containing target alter IDs.}

\item{ego_name}{Name of the ego node in the igraph object. Default is "ego".}

\item{community_attr}{Vertex attribute indicating alter language community.
Default is "languageUsedCategory".}

\item{unknown_community}{Label used for missing alter language community.
Default is "Unknown".}

\item{participant_col}{Optional column in \code{ego_df} to merge into output.
Default is "participant".}
}
\value{
A data frame with one row per ego containing the raw language
betweenness score, the number of valid alter pairs, and the normalized
betweenness score.
}
\description{
This function builds an egor object from cleaned ego, alter, and edge
tables, converts each ego network to an igraph object, and computes
ego language betweenness for every ego using
\code{ego_language_betweenness_single()}.
}
\details{
Betweenness is computed as the extent to which the ego lies on shortest
paths between alters belonging to different language communities,
considering only alter pairs with non-missing community assignments.

In addition to the raw betweenness score, the function also returns the
number of valid alter pairs used in the calculation and a normalized
betweenness score, defined as raw betweenness divided by the number of
valid cross-community alter pairs.
}
